package app.repository;

import app.entity.AssemblyHardware;
import org.springframework.data.repository.CrudRepository;

public interface AssemblyHardwareRepo extends CrudRepository<AssemblyHardware, Integer> {
}
